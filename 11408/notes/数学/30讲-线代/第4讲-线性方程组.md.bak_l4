---
type: 笔记
course: 数学
book: 30讲-线代
lecture: 4
topic: "线性方程组"
status: 未开始
mastery: 0
tags:
  - 考研数学/线代
  - 笔记
source: "[[11408/split/数学/30讲-线代/第4讲-线性方程组]]"
gen: qwen3.5-397b-a17b-think
---

# 第4讲 线性方程组

> [!abstract] 一句话总结
> 线性方程组的求解本质是将向量线性表示问题转化为矩阵秩的讨论，通过初等行变换确定自由变量，利用基础解系刻画解空间结构。

## 核心考点
### 考点1：方程组与向量组的等价性
**线性方程组 $Ax=b$ 的解向量分量即为列向量组 $\boldsymbol{\beta}$ 由 $\boldsymbol{\beta}=\boldsymbol{\beta}$ 线性表示的组合系数。**
方程组 $x_{1}\boldsymbol{\beta}_{1}+x_{2}\boldsymbol{\beta}_{2}+\boldsymbol{\beta}_{n}=\boldsymbol{\beta}$ 与向量组线性表示问题是同一问题的两种表现形式。解向量 $\boldsymbol{x}=[x_{1}, x_{2}, \boldsymbol{\beta}_{n}]^{\boldsymbol{\beta}$ 的系数。

> [!tip] 考试怎么用
> 遇到抽象方程组求解，立即转化为向量线性相关性或线性表示问题思考。

### 考点2：齐次方程组解的判定
**齐次方程组 $Ax=0$ 有非零解的充要条件是系数矩阵秩 $r(A) < n$（未知量个数）。**
若 $r(A)=n$，只有零解；若 $r(A)=r<n$，有无穷多解，且线性无关解的个数为 $n-r(A)$。

> [!tip] 考试怎么用
> 注意 $m<n$ 时必有非零解；$m \boldsymbol{\beta}$ 时不一定，需看秩。

### 考点3：基础解系的定义与性质
**基础解系需满足三个条件：是解、线性无关、任一解均可由其线性表示。**
设 $\boldsymbol{\beta}_{1}, \boldsymbol{\beta}_{2}, \boldsymbol{\beta}_{n-r}$ 是 $Ax=0$ 的基础解系，则通解为 $\boldsymbol{x}=k_{1}\boldsymbol{\beta}_{1}+\boldsymbol{\beta}_{2}+\boldsymbol{\beta}_{n-r}$。

> [!tip] 考试怎么用
> 判断一组向量是否为基础解系，必须同时验证这三个条件，缺一不可。

### 考点4：非齐次方程组解的结构
**非齐次通解等于对应齐次通解加上非齐次的一个特解。**
若 $\boldsymbol{\beta}$ 是 $Ax=b$ 的特解，$\boldsymbol{\beta}_{1}, \boldsymbol{\beta}_{2}, \boldsymbol{\beta}_{n-r}$ 是 $Ax=0$ 的基础解系，则通解为 $\boldsymbol{x}=k_{1}\boldsymbol{\beta}_{1}+\boldsymbol{\beta}_{2}+\boldsymbol{\beta}_{n-r}+\boldsymbol{\beta}$。

> [!tip] 考试怎么用
> 求特解时令自由变量全为 0 最简便；两非齐次解之差必为齐次解。

### 考点5：非齐次方程组有解判定
**非齐次方程组 $Ax=b$ 有解的充要条件是 $r(A)=r([A, \boldsymbol{\beta}])$。**
若 $r(A) \boldsymbol{\beta}$ 无解；若 $r(A)=r([A, \boldsymbol{\beta}])=n$ 唯一解；若 $r(A)=r([A, \boldsymbol{\beta}])<n$ 无穷多解。

> [!tip] 考试怎么用
> 行满秩 ($r(A)=m$) 时，$Ax=b$ 必有解；列满秩 ($r(A)=n$) 时，若有解则唯一。

### 考点6：公共解的求解方法
**求两个方程组的公共解，等价于求解联立后的新方程组。**
方法一：联立 $\begin{bmatrix} A \ B \boldsymbol{\beta}$ 求解；方法二：将一个通解代入另一个方程组定参数；方法三：令两通解相等求解参数。

> [!tip] 考试怎么用
> 若已知两方程组基础解系，令 $k_{1}\boldsymbol{\beta}_{1}+\boldsymbol{\beta}_{2}=l_{1}\boldsymbol{\beta}_{1}+\boldsymbol{\beta}_{2}$ 求解最通用。

### 考点7：同解方程组的判定
**两齐次方程组同解等价于秩相等且一个方程组的解满足另一个。**
$Ax=0$ 与 $Bx=0$ 同解 $\boldsymbol{\beta}$ 且 $Ax=0$ 的解满足 $Bx=0$；或 $r(A)=r(B)=r(\begin{bmatrix} A \ B \boldsymbol{\beta}$。

> [!tip] 考试怎么用
> 证明同解常用于证明秩相等，如 $r(A)=r(A^T A)$。

### 考点8：重要秩等式与同解应用
**实矩阵 $A$ 满足 $r(A)=r(A^T)=r(A^T A)=r(A A^T)$。**
基于 $Ax=0$ 与 $A^T Ax=0$ 同解可推导秩相等；$Ax=b$ 有解时 $A^T x=0$ 与 $\begin{bmatrix} A^T \ \boldsymbol{\beta}^T \boldsymbol{\beta}$ 同解。

> [!tip] 考试怎么用
> 看到 $A^T A$ 立即联想秩不变及同解性质，常用于证明题。

## 易错点
**齐次与非齐次解的判定混淆**

|方程组类型|唯一解条件|无穷多解条件|无解条件|
|---|---|---|---|
|齐次 $Ax=0$|$r(A)=n$|$r(A)<n$|不可能无解（必有零解）|
|非齐次 $Ax=b$|$r(A)=r([A,b])=n$|$r(A)=r([A,b])<n$|$r(A) \boldsymbol{\beta}$|

- **基础解系个数与自由变量**：基础解系向量个数 $s$ 等于自由变量个数，公式为 $s=n-r(A)$。易错点：$n$ 是未知量个数（列数），不是方程个数（行数）。若 $A$ 为 $m \boldsymbol{\beta}$，$n$ 仍取列数。
- **同解与公共解的区别**：公共解是指同时满足两个方程组的解，可能是零解或非零解，求法是联立；同解是指两个方程组的解集合完全相同，要求解空间维数相同且互相包含，常用于证明秩相等。

## 闪卡（Spaced Repetition）
#card
- 方程组 $Ax=b$ 的解向量各分量在向量组视角下代表什么？::代表 $\boldsymbol{b}$ 由 $A$ 的列向量线性表示的组合系数
- 齐次方程组 $Ax=0$ 有非零解等价于 $A$ 的列向量组具有什么性质？::等价于 $A$ 的列向量组线性相关
- 非齐次方程组 $Ax=b$ 无解的几何意义是什么？::向量 $\boldsymbol{b}$ 不能由 $A$ 的列向量组线性表示
- 齐次方程组 $Ax=0$ 仅有零解的充要条件是什么？::系数矩阵列满秩，即 $r(A)=n$
- 若 $A$ 是 $m \times n$ 矩阵且 $m<n$，则 $Ax=0$ 必有何种解？::必有非零解（无穷多解）
- 齐次方程组基础解系中含有的向量个数公式是什么？::$s = n - r(A)$
- 基础解系中的向量必须满足哪三个核心条件？::是解、线性无关、能表示任意解
- 若 $\boldsymbol{\beta}_{1}, \boldsymbol{\beta}_{2}$ 是基础解系，$\boldsymbol{\beta}_{1}+\boldsymbol{\beta}_{2}, \boldsymbol{\beta}_{1}-\boldsymbol{\beta}_{2}$ 是否也是？::是，因为它们线性无关且仍是解
- 基础解系中向量个数少于 $n-r(A)$ 能否表示所有解？::不能，无法张成整个解空间
- 非齐次方程组 $Ax=b$ 的通解结构公式是什么？::齐次通解 + 非齐次特解
- 若 $\boldsymbol{\beta}_{1}, \boldsymbol{\beta}_{2}$ 是 $Ax=b$ 的解，则 $\boldsymbol{\beta}_{1}-\boldsymbol{\beta}_{2}$ 是什么？::是对应齐次方程组 $Ax=0$ 的解
- 如何快速构造非齐次方程组的一个特解？::令所有自由变量为 0，解出主元变量
- 非齐次方程组 $Ax=b$ 无解的秩的判据是什么？::$r(A) \boldsymbol{\beta}$
- 若 $A$ 是 $m \times n$ 矩阵且行满秩 ($r(A)=m$)，则 $Ax=b$ 解的情况？::必有解（可能唯一或无穷多）
- 增广矩阵 $[A, \boldsymbol{\beta}]$ 的秩比 $A$ 的秩大 1 意味着什么？::方程组无解
- 求 $Ax=0$ 与 $Bx=0$ 公共解的最直接方法是什么？::求解联立方程组 $\begin{bmatrix} A \ B \boldsymbol{\beta}$
- 若已知 $Ax=0$ 通解，如何求其与 $Bx=0$ 的公共解？::将 $Ax=0$ 通解代入 $Bx=0$ 解出参数
- 两个齐次方程组的公共解构成的集合是什么？::仍是一个齐次线性方程组的解空间
- 判断 $Ax=0$ 与 $Bx=0$ 同解的充分必要条件之一是什么？::$r(A)=r(B)$ 且 $Ax=0$ 的解都是 $Bx=0$ 的解
- 若 $Ax=0$ 与 $Bx=0$ 同解，则 $r(A)$ 与 $r(B)$ 关系如何？::$r(A) = r(B)$
- 方程组 $Ax=0$ 与 $A^T Ax=0$ 是否同解？::是，对于实矩阵 $A$ 恒同解
- 对于实矩阵 $A$，$r(A)$ 与 $r(A^T A)$ 的关系是？::$r(A) = r(A^T A)$
- 若 $Ax=b$ 有解，则 $A^T x=0$ 与哪个方程组同解？::$\begin{bmatrix} A^T \ \boldsymbol{\beta}^T \boldsymbol{\beta}$
- 利用同解证明 $r(A)=r(A^T A)$ 的关键步骤是什么？::证明 $Ax=0$ 与 $A^T Ax=0$ 解集完全相同

## 复习清单
- [ ] 通读原文 [[第4讲-线性方程组]]（拆分稿）
- [ ] 重做例题：例4.1、例4.2、例4.3、例4.4、例4.5、例4.6、例4.7、例4.8、例4.9、例4.10、例4.11、例4.12、例4.13、例4.14、例4.15
- [ ] 1000题 对应章节

## 关联
- 原文：[[第4讲-线性方程组]]（拆分稿）
