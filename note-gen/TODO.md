# 已知问题与待办（暂缓）

> 用户已知晓，有意暂缓：等架构稳定、问题攒够了再统一处理。不要单独逐条处理，除非用户明确要求。

## 已知遗留（暂不处理）

### 1. `source:` 字段自指（旧管线遗留）

部分讲级笔记的 `source:` 指向笔记自身而非拆分稿原文。全量重跑 `make_notes.py` 时会重新生成 frontmatter，此问题会被自然覆盖，暂不做单独修复。

## 相关背景

- `_notes_runtime/` 顶层运行时目录（2026-08-30 迁移），详情见 `note-gen/README.md`。
- 本文件与笔记数据库无关，是仓库内待办，可随时归档。