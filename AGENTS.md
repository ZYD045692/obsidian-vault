# AGENTS.md

这是我的个人知识库。约定：**每个知识库的入口文件都叫 `<知识库名>.md`**，该库的完整结构与定位规则都写在入口里——如 11408 的入口即 `11408/11408.md` 。

**目前的知识库：**

1. **11408**（408 计算机统考 + 数学一）：内容链路 `pdf/ → books/ → split/ → notes/` ，由 OCR + 清洗流程转换而来，处于持续整理与纠错阶段。

## 任务路由表

> 表中 `<知识库名>` 是占位符，指知识库名；如 `11408/11408.md` 。

### 产线（改 `books/` 、`split/` ）

| 任务 | 先读 |
|---|---|
| 新书入库 / OCR 清洗 / 拆分 / 完整性校验 / 公式修复 | `pipeline/README.md`（专项问题见 `pipeline/guides/` ） |
| VLM 多模态审计（插图鉴定、题目答案完整性、小节对照） | `pipeline/README.md` → `_vlm/PLAN.md` + `_vlm/results/README.md` |

### 笔记（改 `notes/` ）

| 任务 | 先读 |
|---|---|
| 批量生成/填充笔记、知识图谱互链 | `note-gen/README.md` |
| 改单篇笔记、写闪卡、改 frontmatter | `note-gen/笔记约定.md` |
| 更新学习状态（`status` / `mastery` ） | 同上，只改对应字段，勿动其他字段 |
| 闪卡加入/移出 SR 复习队列 | `note-gen/README.md` →「闪卡与 SR 复习队列」 |

### 问答（只读，不改文件）

| 任务 | 先读 |
|---|---|
| 问答、解释概念、对比考点、出练习题 | **按下方「查知识点的顺序」逐层定位**：该库入口 `<知识库名>/<知识库名>.md` →「内容定位规则」→ 科目 MOC → 章节目录 → 再读该章的原文与笔记 |
| 找某个考点/章节对应哪个文件 | 该库入口 `<知识库名>/<知识库名>.md` →「内容定位规则」 |

### 仓库维护

| 任务 | 先读 |
|---|---|
| 有哪些暂缓项、哪些结论已定 | `TODO.md` |
| 重命名 / 移动文件或目录 | 见下方红线 3 |

**批量脚本运行前必须先读对应 README。**

## 查知识点的顺序（先逐层定位，最后才搜索）

回答问题和找知识点时**按层收窄，不要一上来就全库搜**：

1. **知识库**：是哪个知识库的？
2. **科目**：哪一科？408 = 数据结构 / 操作系统 / 计算机组成原理 / 计算机网络；数学 = 高数 / 线代 / 概统。
3. **章节**：读该科目的 MOC 拿章节目录——**MOC 就是章节索引，不在别处另找**：
   - 408：`<知识库名>/notes/408/<科目>/<科目>-MOC.md` （如 `11408/notes/408/计算机组成原理/计算机组成原理-MOC.md` ）
   - 数学：`<知识库名>/notes/数学/30讲-高数/高数-MOC.md` 、`<知识库名>/notes/数学/30讲-线代/线代-MOC.md`
   - 概统没有单独 MOC，章节直接列在 `<知识库名>/notes/数学-MOC.md`
4. **章内**：只在该章节目录里找。**优先 `split/` 的原文与题目**——`<知识库名>/split/<科目>/<N-章名>/考点/x.y-*.md`（原文定义/推导）和 `习题/x.y-本节习题与解析.md`（题目+解析）；需要总结、对比、错题对照时再看 `<知识库名>/notes/<...>/<N-章名>/x.y-*.md` 的考点笔记。

> **为什么 split 优先**：`split/` 是从 `books/`（唯一事实来源）拆出的**原文**，含题目与解析；`notes/` 是模型生成的**总结**，可能不全或有偏差。回答以原文为准，总结只作辅助。

5. **兜底**：前 4 步定不下来时（概念名与章节标题差得远、或横跨多科）才用 Glob / grep 全库搜。**全库搜是兜底，不是第一步**；任何情况下都**不要猜完整路径**。

> **正例**：问「PC 计数器」→ 知识库 `11408` → 科目「计算机组成原理」→ 从 `计算机组成原理-MOC.md` 定到第 5 章中央处理器 → **先读 `split/计算机组成原理/5-中央处理器/考点/5.1-CPU 的功能和基本结构.md` （原文）**，再拿 `notes/408/计算机组成原理/5-中央处理器/5.1-CPU 的功能和基本结构.md` （总结）对照。
> **反例**：直接 `grep 程序计数器` 全库 → 命中 218 处，其中 `books/` 与 `split/` 是同一内容的多份副本，绝大部分是噪声。

## 文档约定

- **每个知识库的入口文件 = `<知识库名>.md`**，该库的结构写在这份入口里（如 `11408/11408.md` ）。知识库内**不新建 README**，下级用各 `-MOC.md` 承载。
- **工具目录（`pipeline/` 、`note-gen/` 、`_vlm/` ）用各自的 README/PLAN** 承载自己的结构。
- **本文件不放目录列表**：需要"这里有什么"时，按上表索引到对应文档，由那份文档给出细节。

## 不可协商的红线

1. **`books/` 是 Markdown 层面的唯一事实来源**（每个知识库各有一份 `<知识库名>/books/` ，如 `11408/books/` ）：OCR 产物直接在 books 上清洗修复；**不在拆分稿上做内容级修补**；改了 books 必须重跑拆分 + 校验（流程见 `pipeline/README.md` ）。
2. **`split/` 与 `notes/` 是产物目录**：不在其中放说明文档。`notes/` 允许人工订正内容，但格式仍须遵守 `note-gen/笔记约定.md` 。
3. **重命名 / 移动文件前先查链接**：检查并修复 wikilink 与 `source:` 字段；需同步的文档有各知识库入口 `<知识库名>/<知识库名>.md` 、`pipeline/README.md` 、`note-gen/README.md` 、`note-gen/笔记约定.md` ，以及脚本内的路径常量（`BASE_SPLIT` / `BASE_NOTE` 等）。
4. **保留 frontmatter 关键字段**：`type` 、`course` 、`section` 、`source` 等 Dataview 与来源追踪依赖字段不得删除。
5. **批量修改先备份**：批量脚本大多会自动备份到 `_vlm/md_backup/` ，执行前仍须确认脚本 docstring 说明。
6. **不提交敏感信息与大文件**：`.gitignore` 已排除 PDF、`_vlm/.env`（API key）及 `_vlm/` 、`_notes_runtime/` 的运行时产物。
7. **公式的输出格式**——写笔记、答题、生成任何内容时都必须遵守：
   - 行内用 `$...$` ，独立成行用 `$$...$$` ；**不使用 `\( \)` / `\[ \]`** 这类 LaTeX 原生定界符。
   - `$` 必须**紧贴内容**：写 `$x$` ，不要写 `$ x $` 。
   - **两个行内公式相邻时必须留空格**：写 `$a$ $b$` ，不要写 `$a$$b$` 。
   - `$$` **独占一行**，前后各留一个空行；**闭合 `$$` 后不得紧跟正文**。
   - **块内不再嵌行内 `$`**。
   - 公式内需要间距用 `\quad` 、`\,` ，**不用全角空格** `　` 。
   - 命令必须完整：`\frac` 、`\left` ，不要出现 `\rac{` 、`\lef` 这类截断形态。
   - 明细与查修：`pipeline/guides/formula-boundary-and-blocks.md` 、`pipeline/guides/数学块闭合-尾随文字.md` 。
8. **临时脚本与临时文件一律放 `tmp/`**（仓库根，已在 `.gitignore` 里）：一次性的调试脚本、扫描输出、中间文件**不要落在** `pipeline/` 、`note-gen/` 、`11408/` 或仓库根