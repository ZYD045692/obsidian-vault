# AGENTS.md

This file provides guidance to AI coding assistants when working with this repository.

## 项目概述

这是一个 Obsidian 个人知识库，用于整理中国研究生入学考试 **408 计算机统考** 与 **考研数学** 的学习资料。内容由 PDF 扫描版经 OCR + 清洗流程转换而来，目前处于持续整理与纠错阶段。

- **入口**：`11408/首页.md`
- **核心内容**：`11408/` 下的源库（books）、拆分稿、学习笔记

## 意图路由（先判断任务属于哪一类，再去对应文档）

| 用户意图                              | 怎么做                                                                                                                              |
| --------------------------------- | -------------------------------------------------------------------------------------------------------------------------------- |
| 问答、解释概念、对比考点、出练习题                 | 直接读 `11408/notes/` 与 `11408/split/` 相关内容回答，**不改文件**                                                                                   |
| 改单篇笔记（补内容、修错、记错题、写闪卡）             | 直接改 `11408/notes/` 下对应文件，遵守下方「笔记约定」                                                                                                 |
| 更新学习状态（status / mastery）          | 只改 frontmatter 对应字段，勿动其他字段                                                                                                       |
| 把闪卡加入/移出 SR 复习队列                 | 运行 `python note-gen/toggle_card_tag.py --on|--off 路径/glob`；笔记生成物默认用 `#card`（不进入复习），确认后再切 `#flashcards`（详见下方「笔记约定」闪卡说明）                                      |
| 重命名 / 移动文件或目录                     | 改完必须批量检查 wikilink 与 `source:` 字段；并同步更新：本文件目录树、`pipeline/README.md` 与 `note-gen/README.md` 的目录结构、相关脚本内的路径常量（`BASE_SPLIT` / `BASE_NOTE` 等） |
| 新书入库 / OCR 清洗 / 拆分 / 完整性校验 / 公式修复 | **先读 `pipeline/README.md`**，按其中 6 步流水线执行；专项问题查 `pipeline/guides/`                                                            |
| 批量生成/填充笔记、知识图谱互链                  | **先读 `note-gen/README.md`**，按其中脚本说明执行                                                                                          |
| VLM 多模态审计（插图鉴定、题目答案完整性、小节对照）      | **先读 `_vlm/PLAN.md` 与 `_vlm/results/README.md`**                                                                                 |

**批量脚本运行前必须先读对应 README。** pipeline 脚本在 `pipeline/scripts/`，笔记生成入口 `note-gen/make_notes.py`，逻辑在 `note-gen/core/`。

## 目录结构

```
.
├── CLAUDE.md          # 导入 AGENTS.md（Claude Code 入口）
├── AGENTS.md          # 本文件
├── pipeline/          # 内容生产：OCR → books → 拆分稿 的规范与脚本（详见 pipeline/README.md）
├── note-gen/          # 笔记生成：拆分稿 → 笔记 的规范与脚本（详见 note-gen/README.md）
├── cropped-pdf/       # 中间产物：扫描 PDF 去页眉页脚后的分章 PDF（从 11408/pdf/ 可再生）
├── _vlm/              # VLM 多模态审计流水线（详见 _vlm/PLAN.md）
├── _notes_runtime/    # 笔记生成的运行时产物（raw 留档 / 失败清单 / 日志）
└── 11408/             # 考研资料统一目录（内容层）
    ├── 首页.md        # 知识库总入口
    ├── books/         # 全书本源库：OCR 产物直接在此清洗修复，一本书一个目录（书内 .md + imgs/）
    ├── pdf/           # PDF 扫描原书（408/ 与 数学/）
    ├── split/         # 从 books 拆出的原文（含题目）
    └── notes/         # 知识点总结（最终产物）
```

### 拆分稿（原文 + 题目）

```
split/
├── 408-MOC.md  数学-MOC.md
├── 数据结构/ 操作系统/ 计算机组成原理/ 计算机网络/   # 408 四科，结构相同
│   └── N-章名/                     # 如 1-绪论（编号不补前导 0）
│       ├── N-章名-导读.md          # 章导读
│       ├── 考点/x.y-节名.md         # 如 1.1-数据结构的基本概念.md
│       └── 习题/x.y-本节习题与解析.md
└── 数学/
    ├── 30讲-高数/  30讲-线代/       # 第N讲-讲名.md（整讲一个文件）
    ├── 概统-基础/  概统-强化/       # 李良概率统计讲义，第N章-章名.md（整章一个文件）
    └── 1000题-试题册/  1000题-解析册/
        └── 基础篇|强化篇/高数|线代|概率/第N章 章名.md   # 综合篇为 测试卷一~四.md，直接放篇目录下
```

### 笔记（知识点总结）

```
notes/
├── 408-MOC.md  数学-MOC.md
├── 408/
│   └── 数据结构/ 操作系统/ 计算机组成原理/ 计算机网络/
│       ├── <课程>-MOC.md
│       └── N-章名/x.y-节名.md      # 考点笔记，与拆分稿考点同名
└── 数学/
    ├── 30讲-高数/                  # 第N讲-讲名/ 目录：内放 第N讲-讲名.md（讲级笔记）+ x.y-标题.md（小节笔记）；另有 高数-MOC.md、专题/
    └── 30讲-线代/                  # 只有讲级：第N讲-讲名.md 直接放书目录下；另有 线代-MOC.md、专题/
```

### 内容定位规则（问答时按此找文件）

- 用户问「数据结构 1.1」→ 笔记在 `notes/408/数据结构/1-绪论/1.1-*.md`；原文在 `split/数据结构/1-绪论/考点/1.1-*.md`；题目在同级 `习题/`
- 408 考点编号 `x.y` 中 x = 章号，与 `N-章名` 目录的 N 一致
- 数学小节 `x.y` 中 x = 讲号，原文整讲在 `split/数学/30讲-*/第x讲-*.md`；小节笔记只有高数有，在 `notes/数学/30讲-高数/第x讲-讲名/x.y-*.md`；线代只有讲级笔记 `notes/数学/30讲-线代/第x讲-讲名.md`
- 概统讲义：原文整章在 `split/数学/概统-基础|概统-强化/第N章-章名.md`；用户问「概统基础 第三章」即定位到对应章文件
- 1000 题按「篇 → 科 → 章」三级定位，试题册与解析册目录结构对称
- 拿不准文件名时用 Glob（如 `notes/**/1.1-*.md`），不要猜完整路径

### 笔记 vs 拆分稿的职责

- **`11408/notes/`**：只放**知识点总结**（讲义、考点梳理）。
- **`11408/split/`**：放拆分后的书籍内容，包括正文、考点和**题目**（1000 题、模拟卷、真题等做题资料继续放在这里）。
- 未来若题目资料规模扩大，再考虑单独建 `11408/题库/` 或 `11408/试卷/`。

核心原则：内容链路为 `pdf/（扫描原书）→ books/（OCR 提取 + 清洗修复的文本底本）→ split/`。在 Markdown 层面 books 是唯一事实来源：拆分稿从 books 一次性生成，**不在拆分稿上做内容级修补**；改了 books 必须重跑拆分 + 校验；books 内容有疑义时回查 `pdf/` 原书。

## 笔记约定

### Frontmatter 规范

学习笔记（`11408/notes/`）通常包含以下 YAML 字段：

```yaml
---
type: 考点          # 可选：MOC / 考点 / 习题 / 小节 / 笔记
course: 数据结构     # 课程名
chapter_no: 1
chapter_title: 绪论
section: "1.1"
point_title: "1.1 数据结构的基本概念"
status: 未开始       # 学习状态：未开始 / 学习中 / 已完成
mastery: 0          # 掌握度 0-100
tags:
  - 408/数据结构
  - 考点
source: "[[11408/split/数据结构/1-绪论/考点/1.1-数据结构的基本概念]]"
gen: qwen3.5-35b-a3b-think
---
```

- `type` 决定 Dataview 统计口径，不要随意改动。
- `source` 指向拆分稿中的原文位置，保持其为有效 wikilink。
- frontmatter 已有 `gen:` 的笔记视为已生成，批量填充脚本会跳过。

### 内容区块

考点笔记常见固定区块：

- `## 核心要点` — 用 `**考点 N：标题**` 分点
- `## 易混与对比` — 表格形式对比概念
- `## 真题与错题` — 链接到拆分稿的习题解析
- `## 关联考点` — 前置/后续/易混 wikilink
- `## 闪卡` — Obsidian Spaced Repetition 格式：`问题::答案`。
  标签使用 `#card`，**不是 `#flashcards`**：SR 插件默认识别 `#flashcards`，若生成物直接用 `#flashcards`，所有新卡片会立即涌入复习队列，无法做批量质量控制。用 `#card` 相当于"待机"状态，确认想复习某篇/某批后，运行 `python note-gen/toggle_card_tag.py --on 路径` 切换到 `#flashcards` 即可被 SR 扫描；反之可 `--off` 移出复习队列。

### 链接与引用

- 笔记内部链接统一使用 Obsidian wikilink：`[[路径/文件名]]` 或 `[[路径/文件名|显示名]]`
- 数学公式使用 `$...$` 行内或 `$$...$$` 块级 LaTeX
- 图片统一用 wikilink：`![[图片名.png]]`
- 流程文档（本文件、两个 README、guides/）不属于笔记数据库，互相引用用普通路径，不用 wikilink

### Dataview 查询

MOC 页用 Dataview 做统计，例如：

```dataview
TABLE course AS 课程, length(rows) AS 考点数
FROM "11408/notes"
WHERE type = "考点"
GROUP BY course
SORT course ASC
```

修改笔记时注意不要破坏 Dataview 查询所需的 frontmatter 字段。

## 工作规则

1. **`11408/books/` 是 Markdown 层面的唯一事实来源**（上游是 `pdf/` 扫描原书）：OCR 产物直接在 books 上清洗、修复（去广告/统一标题/修公式等），直到质量达到能一次性生成拆分稿的程度；后续发现公式等问题也继续改 books。改完必须重跑拆分 + 校验，**不在拆分稿上做内容级修补**（规范见 `pipeline/README.md`）。
2. **批量修改先备份**：批量脚本大多会自动备份到 `_vlm/md_backup/`，但执行前仍需确认脚本 docstring 说明。
3. **保持 wikilink 有效**：重命名或移动笔记时，检查并修复相关 wikilink。
4. **保留 frontmatter**：修改考点/习题笔记时，不要删除 `type`、`course`、`section`、`source` 等 Dataview 与来源追踪依赖字段。
5. **不要向 git 提交敏感信息与大文件**：`.gitignore` 已排除 PDF、`_vlm/.env`（API key），以及 `_vlm/` 与 `_notes_runtime/` 的运行时产物（页面截图、日志、md_backup、模型 raw 留档等）。
6. **VLM 结果只读**：`_vlm/results/` 下的 JSONL 与 Markdown 观察表是模型观察记录，人工裁定后再决定是否修改正文；不要为了让结果变"好看"而反向改 JSONL。

## 注意事项

- 很多脚本是为**特定清洗阶段**写的一次性工具（文件名常含课程缩写或轮次，如 `_fix_ti_round2.py`）。再次运行前请确认其目标文件和阶段是否仍然适用。
- 数学笔记中的 LaTeX 公式较多，批量替换 `$` 或 `\` 时要特别小心公式边界。
- 题库类笔记（1000 题、408 习题）有严格的题号体系，插入或删除题目会破坏题↔答对应关系。
