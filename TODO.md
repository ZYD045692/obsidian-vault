# 待办

> 仓库级待办，与笔记数据库无关（流程文档，互相引用用普通路径，不用 wikilink）。
> **暂缓项在用户明确要求动手前，不要自行推进。**

## 暂缓：自己的错题记录结构

**状态**：未想好，先记下（2026-09-14）。**待设计**。

**要解决的问题**：目前缺一个存放「自己做错的题」的结构，用途是提醒自己哪些地方容易做错。
上游 `11408.skill` 提供的是作者预设的经验型易错清单——那是「猜你会错什么」；我们要的是「你在哪错过」，记录事实，再由查询产生提醒。方向相反，不能拿它的内容。

**已实测的两条硬约束**（重新设计时不必再查）：

1. `split/**/习题/*.md` 里题号不是唯一锚点：`##### 01` 在同一文件内出现 4 次（题目·单选 / 题目·综合 / 解析·单选 / 解析·综合）。
   所以**指针只能精确到文件级**，具体题目只能用「年份 + 题型 + 题号」文本定位。
2. 题干与解析在同一文件内（`### x.y.4 答案与解析`），所以**不需要复制题干**，指针足够。
   例外：数学 1000 题试题册与解析册分离，需要两个指针。

**待定的三个岔路口**：

1. **粒度**：一节一文件（倾向，镜像 `split` 的习题目录）/ 一题一文件 / 一考点一文件。
2. **提醒方式**：Dataview 看板被动查看 / 复用 `#card` → `#flashcards` 做 SR 主动推送 / 两者。
3. **收纳范围**：只记错题 / 也记「做对了但不放心、没拿满分」的题。

**已确定的设计原则**（若动手，按此执行）：

- 错题记录只存「与标准答案的差」，题干留在 `split`。
- **不预设错因分类字段**（概念不清 / 方法不会 / 计算失误那套），用自己的话写批注；分类等积累到量再由数据归纳。
- 订正过的错题**不删除**，只改状态——删掉等于抹掉频次证据，「我容易做错」的统计会失真。
- 新增独立层，不碰 `11408/books`、`11408/split`、`11408/notes`。

**相关背景**：

- `split/**/习题/` 内有 798 条带年份标签的 `【YYYY 统考真题】`，覆盖 2009–2025。**全部落在「题目」区（解析区 0 条），无题目/解析双计。**
- 按题型核对（应有 17 年 ×（40 单选 + 7 综合）= 799）：**综合应用题每年恰好 7 道、共 119 道，零偏差**；单选有 4 个年份出入，去重后约缺 3 道：
  - 2016 标了 41 道单选，其中 1 道跨学科重复（计组 `5.5` 与 OS `1.3` 收了同一道「异常/中断」题）→ 去重后 40，正确；
  - 2023 标了 40 道，但计组内部有 1 道重复（`4.3` 与 `3.6` 同收一道「请求调页/虚拟存储」题）→ 去重后 39，缺 1；
  - 2013 为 39（缺 1）、2024 为 38（缺 2）、2021 为 41（计组 12 道，正常 11，疑似误标或多 1）。
- 结论：**可作频次统计的底本，但统计前必须先做一次去重 + 补漏校对**。缺的题在习题区没有找到未标注的候选，可能是书本身没收；不影响「按考点累计真题年份」这个方向。
- 数学面目前没有任何带标签的真题（`【…真题…】` 命中 0 条）。

## 已评估并排除：上游 11408.skill（结论留档）

克隆在 `11408.skill/`（MIT，已加入 `.gitignore`，只作本地参考）。逐项评估后**内容层基本全部排除**，只留组织形式。以下是已定论，不要重复评估：

- **考频 / 考点优先级**：原作者从未说明来源；仓库自述为「使用先进的LLM炼化考点」，即 LLM 生成的印象值，且 roadmap 里仍留着「待校准」。
  我们自己的 798 条真题可算出**可回查的**频次，不必抄它。
- **知识讲解 / 考点精析**：与我们 `split`、`notes` 重叠，且深度不如我们，不拿。
- **六大题解题模板**：我们会自己总结笔记，不拿。
- **时间分配数字**：通用常识，不值得单独成文。
- **常见陷阱清单**：性质是「作者预设」而非「我的实际错误」，只能当错因标签候选，不当内容。
- **答题书写规范**：与回答模板构成两套并行的格式说明，互相打架；且部分内容（如数学证明写「构造动机」）不匹配真实作答。
- **错题分析三步框架**：用户自会思考，不预设三类错因。
- **刷题节奏五阶段表**：考研圈共识，无独创信息。
- **Obsidian 笔记写回流程**：已否（该笔记存在较大问题）。

**仍可借鉴的组织形式**（仅此四类）：

1. 路由层不塞知识的**分层结构**（路由文件只回答「什么信号 → 读哪个文件」）。
2. 每个参考文件开头放**目录**，便于按需定位。
3. **稳定 id 解耦**：考点用 id 而非中文名/路径引用（值用自己的数据算）。
4. 若将来要对外分发 skill：它的 **runtime 安装目录表**（`~/.codex/skills/`、`~/.claude/skills/`、`~/.cursor/skills/`）。